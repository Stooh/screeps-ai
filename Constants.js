var Constants = {};

// height and width of every room
Constants.ROOM_SIZE = 50;

// distance per room heuristic when 2 objects aren't in the same room
Constants.DISTANCE_PER_ROOM = Constants.ROOM_SIZE;

module.exports = Constants;
